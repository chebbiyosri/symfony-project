{% form_theme form 'SidesPollBundle:Frontend:form_custom_layout.html.twig' %}

<form action="{{ path('SidesPollBundle_frontend_poll_vote', {'pollId': poll.id }) }}" method="post" novalidate="novalidate" {{ form_enctype(form) }}>

    {{ form_errors(form.opinions) }}
    {{ form_widget(form.opinions) }}

    {{ form_rest(form) }}

    <input type="submit" value="Submit" />
</form>

<a href="{{ path('SidesPollBundle_frontend_poll_results', {'pollId': poll.id }) }}" class="see_results">View results</a>