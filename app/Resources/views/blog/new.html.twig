{% extends 'base.html.twig' %}

{% block body %}
  
        <h1>Blog creation</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="form-group row">
            <div class="col-sm-10">
                <input type="submit" class="btn btn-primary" value="Create" />
                {{ form_end(form) }}

                <a href="{{ path('blog_index') }}" class="btn btn-secondary">Back to the list</a>
            </div>
        </div>
   

{% endblock %}
